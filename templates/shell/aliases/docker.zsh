# =========================================================
# DOCKER / COLIMA ALIASES
# =========================================================

alias dockerclean='docker container prune -f && docker image prune -a -f'
